# Structure

- `init.lua` : Installs all plugins, lsps, treesitter parsers etc. Also defines necessary for vim.pack events, and has autocommands for enabling treesitter parsers.
- `lua/aucmds` : Module that has helper functions for common autocommand types.


